import {
  addDay,
  ancestor,
  assign,
  bgEvent,
  btnTextMonth,
  cloneDate,
  createEventChunk,
  createEventClasses,
  createEventContent,
  datesEqual,
  debounce,
  eventIntersects,
  height,
  helperEvent,
  intl,
  isFunction,
  keyEnter,
  max,
  outsideEvent,
  prepareEventChunks,
  rect,
  repositionEvent,
  resourceBackgroundColor,
  resourceTextColor,
  runReposition,
  setContent,
  setPayload,
  subtractDay,
  task,
  themeView,
  toEventWithLocalDates,
  toISOString,
  toViewWithLocalDates
} from "./chunk-WHGF6NN2.js";
import "./chunk-YUPN7B2T.js";
import {
  derived as derived2,
  writable
} from "./chunk-I5P57XGH.js";
import "./chunk-3CA2SXWX.js";
import {
  action,
  add_locations,
  afterUpdate,
  bind_prop,
  bind_this,
  bubble_event,
  check_target,
  component,
  each,
  hmr,
  if_block,
  index,
  init,
  legacy_api,
  onMount,
  prop,
  set_attribute,
  set_class,
  setup_stores,
  store_get,
  store_mutate,
  store_set,
  validate_each_keys,
  validate_store
} from "./chunk-WQCBTQS3.js";
import {
  append,
  comment,
  stopPropagation,
  template
} from "./chunk-EMKJVXKS.js";
import {
  $window,
  FILENAME,
  HMR,
  apply,
  child,
  deep_read_state,
  derived,
  derived_safe_equal,
  effect,
  event,
  first_child,
  get,
  getContext,
  legacy_pre_effect,
  legacy_pre_effect_reset,
  mark_module_end,
  mark_module_start,
  mutable_state,
  mutate,
  pop,
  push,
  reset,
  set,
  sibling,
  strict_equals,
  template_effect,
  tick,
  untrack
} from "./chunk-7T7Y4H63.js";
import "./chunk-HNWPC2PS.js";
import "./chunk-MBL6SW7X.js";
import "./chunk-RIXFT5AQ.js";
import "./chunk-RVAV4ZRS.js";
import "./chunk-3F74YA3Z.js";

// node_modules/@event-calendar/day-grid/src/stores.js
function days(state) {
  return derived2([state.date, state.firstDay, state.hiddenDays], ([$date, $firstDay, $hiddenDays]) => {
    let days2 = [];
    let day = cloneDate($date);
    let max2 = 7;
    while (day.getUTCDay() !== $firstDay && max2) {
      subtractDay(day);
      --max2;
    }
    for (let i = 0; i < 7; ++i) {
      if (!$hiddenDays.includes(day.getUTCDay())) {
        days2.push(cloneDate(day));
      }
      addDay(day);
    }
    return days2;
  });
}

// node_modules/@event-calendar/day-grid/src/Header.svelte
mark_module_start();
Header[FILENAME] = "node_modules/@event-calendar/day-grid/src/Header.svelte";
var root_1 = add_locations(template(`<div role="columnheader"><span></span></div>`), Header[FILENAME], [[11, 12, [[12, 16]]]]);
var root = add_locations(template(`<div><div role="row"></div> <div></div></div>`), Header[FILENAME], [[8, 0, [[9, 4], [19, 4]]]]);
function Header($$anchor, $$props) {
  check_target(new.target);
  push($$props, false, Header);
  const $$stores = setup_stores();
  const $theme = () => (validate_store(theme, "theme"), store_get(theme, "$theme", $$stores));
  const $_days = () => (validate_store(_days, "_days"), store_get(_days, "$_days", $$stores));
  const $_intlDayHeaderAL = () => (validate_store(_intlDayHeaderAL, "_intlDayHeaderAL"), store_get(_intlDayHeaderAL, "$_intlDayHeaderAL", $$stores));
  const $_intlDayHeader = () => (validate_store(_intlDayHeader, "_intlDayHeader"), store_get(_intlDayHeader, "$_intlDayHeader", $$stores));
  let {
    theme,
    _intlDayHeader,
    _intlDayHeaderAL,
    _days
  } = getContext("state");
  init();
  var div = root();
  var div_1 = child(div);
  each(div_1, 5, $_days, index, ($$anchor2, day) => {
    var div_2 = root_1();
    const class_derived = derived_safe_equal(() => {
      var _a;
      return `${$theme().day ?? ""} ${((_a = $theme().weekdays) == null ? void 0 : _a[get(day).getUTCDay()]) ?? ""}`;
    });
    var span = child(div_2);
    template_effect(() => set_attribute(span, "aria-label", $_intlDayHeaderAL().format(get(day))));
    action(span, ($$node, $$action_arg) => {
      var _a;
      return (_a = setContent) == null ? void 0 : _a($$node, $$action_arg);
    }, () => $_intlDayHeader().format(get(day)));
    reset(div_2);
    template_effect(() => set_class(div_2, get(class_derived)));
    append($$anchor2, div_2);
  });
  reset(div_1);
  var div_3 = sibling(div_1, 2);
  reset(div);
  template_effect(() => {
    set_class(div, $theme().header);
    set_class(div_1, $theme().days);
    set_class(div_3, $theme().hiddenScroll);
  });
  append($$anchor, div);
  return pop({ ...legacy_api() });
}
if (import.meta.hot) {
  Header = hmr(Header, () => Header[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = Header[HMR].source;
    set(Header[HMR].source, module.default[HMR].original);
  });
}
var Header_default = Header;
mark_module_end(Header);

// node_modules/@event-calendar/day-grid/src/Event.svelte
mark_module_start();
Event[FILENAME] = "node_modules/@event-calendar/day-grid/src/Event.svelte";
var root2 = add_locations(template(`<article><!> <div></div> <!></article>`), Event[FILENAME], [[177, 0, [[195, 4]]]]);
function Event($$anchor, $$props) {
  check_target(new.target);
  push($$props, false, Event);
  const $$stores = setup_stores();
  const $resources = () => (validate_store(resources, "resources"), store_get(resources, "$resources", $$stores));
  const $eventBackgroundColor = () => (validate_store(eventBackgroundColor, "eventBackgroundColor"), store_get(eventBackgroundColor, "$eventBackgroundColor", $$stores));
  const $eventColor = () => (validate_store(eventColor, "eventColor"), store_get(eventColor, "$eventColor", $$stores));
  const $eventTextColor = () => (validate_store(eventTextColor, "eventTextColor"), store_get(eventTextColor, "$eventTextColor", $$stores));
  const $theme = () => (validate_store(theme, "theme"), store_get(theme, "$theme", $$stores));
  const $_iClasses = () => (validate_store(_iClasses, "_iClasses"), store_get(_iClasses, "$_iClasses", $$stores));
  const $eventClassNames = () => (validate_store(eventClassNames, "eventClassNames"), store_get(eventClassNames, "$eventClassNames", $$stores));
  const $_view = () => (validate_store(_view, "_view"), store_get(_view, "$_view", $$stores));
  const $displayEventEnd = () => (validate_store(displayEventEnd, "displayEventEnd"), store_get(displayEventEnd, "$displayEventEnd", $$stores));
  const $eventContent = () => (validate_store(eventContent, "eventContent"), store_get(eventContent, "$eventContent", $$stores));
  const $_intlEventTime = () => (validate_store(_intlEventTime, "_intlEventTime"), store_get(_intlEventTime, "$_intlEventTime", $$stores));
  const $eventDidMount = () => (validate_store(eventDidMount, "eventDidMount"), store_get(eventDidMount, "$eventDidMount", $$stores));
  const $eventAllUpdated = () => (validate_store(eventAllUpdated, "eventAllUpdated"), store_get(eventAllUpdated, "$eventAllUpdated", $$stores));
  const $_interaction = () => (validate_store(_interaction, "_interaction"), store_get(_interaction, "$_interaction", $$stores));
  const $_popupDate = () => (validate_store(_popupDate, "_popupDate"), store_get(_popupDate, "$_popupDate", $$stores));
  const $dayMaxEvents = () => (validate_store(dayMaxEvents, "dayMaxEvents"), store_get(dayMaxEvents, "$dayMaxEvents", $$stores));
  const $_hiddenEvents = () => (validate_store(_hiddenEvents, "_hiddenEvents"), store_get(_hiddenEvents, "$_hiddenEvents", $$stores));
  const $eventClick = () => (validate_store(eventClick, "eventClick"), store_get(eventClick, "$eventClick", $$stores));
  const $eventMouseEnter = () => (validate_store(eventMouseEnter, "eventMouseEnter"), store_get(eventMouseEnter, "$eventMouseEnter", $$stores));
  const $eventMouseLeave = () => (validate_store(eventMouseLeave, "eventMouseLeave"), store_get(eventMouseLeave, "$eventMouseLeave", $$stores));
  let chunk = prop($$props, "chunk", 8);
  let longChunks = prop($$props, "longChunks", 24, () => ({}));
  let inPopup = prop($$props, "inPopup", 8, false);
  let dates = prop($$props, "dates", 24, () => []);
  let {
    dayMaxEvents,
    displayEventEnd,
    eventAllUpdated,
    eventBackgroundColor,
    eventTextColor,
    eventClick,
    eventColor,
    eventContent,
    eventClassNames,
    eventDidMount,
    eventMouseEnter,
    eventMouseLeave,
    resources,
    theme,
    _view,
    _intlEventTime,
    _interaction,
    _iClasses,
    _hiddenEvents,
    _popupDate,
    _tasks
  } = getContext("state");
  let el = mutable_state();
  let event2 = mutable_state();
  let classes = mutable_state();
  let style = mutable_state();
  let content = mutable_state();
  let timeText = mutable_state();
  let margin = mutable_state(1);
  let hidden = mutable_state(false);
  let display = mutable_state();
  let onclick = mutable_state();
  onMount(() => {
    if (isFunction($eventDidMount())) {
      $eventDidMount()({
        event: toEventWithLocalDates(get(event2)),
        timeText: get(timeText),
        el: get(el),
        view: toViewWithLocalDates($_view())
      });
    }
  });
  afterUpdate(() => {
    if (isFunction($eventAllUpdated()) && !helperEvent(get(display))) {
      task(() => $eventAllUpdated()({ view: toViewWithLocalDates($_view()) }), "eau", _tasks);
    }
  });
  function createHandler(fn, display2) {
    return !helperEvent(display2) && isFunction(fn) ? (jsEvent) => fn({
      event: toEventWithLocalDates(get(event2)),
      el: get(el),
      jsEvent,
      view: toViewWithLocalDates($_view())
    }) : void 0;
  }
  function createDragHandler(interaction, resize) {
    return interaction.action ? (jsEvent) => $_interaction().action.drag(get(event2), jsEvent, resize, inPopup() ? $_popupDate() : null, [
      rect(get(el)).top - rect(ancestor(get(el), 1)).top,
      dates()
    ]) : void 0;
  }
  function reposition() {
    if (!get(el)) {
      return;
    }
    set(margin, repositionEvent(chunk(), longChunks(), height(get(el))));
    if (strict_equals($dayMaxEvents(), true)) {
      hide();
    } else {
      set(hidden, false);
    }
  }
  function hide() {
    let dayEl = ancestor(get(el), 2);
    let h = height(dayEl) - height(dayEl.firstElementChild) - footHeight(dayEl);
    set(hidden, chunk().bottom > h);
    let update = false;
    for (let date of chunk().dates) {
      let hiddenEvents = $_hiddenEvents()[date.getTime()];
      if (hiddenEvents) {
        let size = hiddenEvents.size;
        if (get(hidden)) {
          hiddenEvents.add(chunk().event);
        } else {
          hiddenEvents.delete(chunk().event);
        }
        if (strict_equals(size, hiddenEvents.size, false)) {
          update = true;
        }
      }
    }
    if (update) {
      store_set(_hiddenEvents, $_hiddenEvents());
    }
  }
  function footHeight(dayEl) {
    let h = 0;
    for (let i = 0; i < chunk().days; ++i) {
      h = max(h, height(dayEl.lastElementChild));
      dayEl = dayEl.nextElementSibling;
      if (!dayEl) {
        break;
      }
    }
    return h;
  }
  legacy_pre_effect(() => deep_read_state(chunk()), () => {
    set(event2, chunk().event);
  });
  legacy_pre_effect(
    () => (get(display), get(event2), resourceBackgroundColor, $resources(), $eventBackgroundColor(), $eventColor(), resourceTextColor, $eventTextColor(), bgEvent, get(style), deep_read_state(chunk()), get(margin), get(hidden), $theme(), $_iClasses(), createEventClasses, $eventClassNames(), $_view()),
    () => {
      set(display, get(event2).display);
      let bgColor = get(event2).backgroundColor || resourceBackgroundColor(get(event2), $resources()) || $eventBackgroundColor() || $eventColor();
      let txtColor = get(event2).textColor || resourceTextColor(get(event2), $resources()) || $eventTextColor();
      if (bgEvent(get(display))) {
        set(style, `width:calc(${chunk().days * 100}% + ${chunk().days - 1}px);`);
      } else {
        let marginTop = get(margin);
        if (get(event2)._margin) {
          let [_margin, _dates] = get(event2)._margin;
          if (chunk().date >= _dates[0] && chunk().date <= _dates.at(-1)) {
            marginTop = _margin;
          }
        }
        set(style, `width:calc(${chunk().days * 100}% + ${(chunk().days - 1) * 7}px);margin-top:${marginTop}px;`);
      }
      if (bgColor) {
        set(style, get(style) + `background-color:${bgColor};`);
      }
      if (txtColor) {
        set(style, get(style) + `color:${txtColor};`);
      }
      if (get(hidden)) {
        set(style, get(style) + "visibility:hidden;");
      }
      set(style, get(style) + get(event2).styles.join(";"));
      set(classes, [
        bgEvent(get(display)) ? $theme().bgEvent : $theme().event,
        ...$_iClasses()([], get(event2)),
        ...createEventClasses($eventClassNames(), get(event2), $_view())
      ].join(" "));
    }
  );
  legacy_pre_effect(
    () => (get(timeText), get(content), createEventContent, deep_read_state(chunk()), $displayEventEnd(), $eventContent(), $theme(), $_intlEventTime(), $_view()),
    () => {
      (($$value) => (set(timeText, $$value[0]), set(content, $$value[1])))(createEventContent(chunk(), $displayEventEnd(), $eventContent(), $theme(), $_intlEventTime(), $_view()));
    }
  );
  legacy_pre_effect(() => ($eventClick(), get(display)), () => {
    set(onclick, createHandler($eventClick(), get(display)));
  });
  legacy_pre_effect_reset();
  init();
  var article = root2();
  var event_handler = derived(() => get(onclick) && keyEnter(get(onclick)));
  var event_handler_1 = derived(() => createHandler($eventMouseEnter(), get(display)));
  var event_handler_2 = derived(() => createHandler($eventMouseLeave(), get(display)));
  var event_handler_3 = derived(() => !helperEvent(get(display)) && createDragHandler($_interaction()));
  var node = child(article);
  var event_handler_4 = derived(() => createDragHandler($_interaction(), ["x", "start"]));
  component(node, () => $_interaction().resizer, ($$anchor2, $$component) => {
    $$component($$anchor2, {
      start: true,
      get event() {
        return get(event2);
      },
      $$events: {
        pointerdown(...$$args) {
          apply(() => get(event_handler_4), this, $$args, Event, [193, 24], true);
        }
      }
    });
  });
  var div = sibling(node, 2);
  action(div, ($$node, $$action_arg) => {
    var _a;
    return (_a = setContent) == null ? void 0 : _a($$node, $$action_arg);
  }, () => get(content));
  var node_1 = sibling(div, 2);
  var event_handler_5 = derived(() => createDragHandler($_interaction(), ["x", "end"]));
  component(node_1, () => $_interaction().resizer, ($$anchor2, $$component) => {
    $$component($$anchor2, {
      get event() {
        return get(event2);
      },
      $$events: {
        pointerdown(...$$args) {
          apply(() => get(event_handler_5), this, $$args, Event, [199, 24], true);
        }
      }
    });
  });
  reset(article);
  bind_this(article, ($$value) => set(el, $$value), () => get(el));
  template_effect(() => {
    set_class(article, get(classes));
    set_attribute(article, "style", get(style));
    set_attribute(article, "role", get(onclick) ? "button" : void 0);
    set_attribute(article, "tabindex", get(onclick) ? 0 : void 0);
    set_class(div, $theme().eventBody);
  });
  event("click", article, function(...$$args) {
    apply(() => get(onclick) || void 0, this, $$args, Event, [183, 14]);
  });
  event("keydown", article, function(...$$args) {
    apply(() => get(event_handler), this, $$args, Event, [184, 16]);
  });
  event("mouseenter", article, function(...$$args) {
    apply(() => get(event_handler_1), this, $$args, Event, [185, 19], true);
  });
  event("mouseleave", article, function(...$$args) {
    apply(() => get(event_handler_2), this, $$args, Event, [186, 19], true);
  });
  event("pointerdown", article, function(...$$args) {
    apply(() => get(event_handler_3), this, $$args, Event, [187, 20]);
  });
  append($$anchor, article);
  bind_prop($$props, "reposition", reposition);
  return pop({
    get reposition() {
      return reposition;
    },
    ...legacy_api()
  });
}
if (import.meta.hot) {
  Event = hmr(Event, () => Event[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = Event[HMR].source;
    set(Event[HMR].source, module.default[HMR].original);
  });
}
var Event_default = Event;
mark_module_end(Event);

// node_modules/@event-calendar/day-grid/src/Popup.svelte
mark_module_start();
Popup[FILENAME] = "node_modules/@event-calendar/day-grid/src/Popup.svelte";
var root3 = add_locations(template(`<div><div><time></time> <a role="button" tabindex="0">&times;</a></div> <div></div></div>`), Popup[FILENAME], [
  [
    80,
    0,
    [
      [88, 4, [[89, 8], [91, 8]]],
      [99, 4]
    ]
  ]
]);
function Popup($$anchor, $$props) {
  check_target(new.target);
  push($$props, false, Popup);
  const $$stores = setup_stores();
  const $_popupChunks = () => (validate_store(_popupChunks, "_popupChunks"), store_get(_popupChunks, "$_popupChunks", $$stores));
  const $_popupDate = () => (validate_store(_popupDate, "_popupDate"), store_get(_popupDate, "$_popupDate", $$stores));
  const $_interaction = () => (validate_store(_interaction, "_interaction"), store_get(_interaction, "$_interaction", $$stores));
  const $theme = () => (validate_store(theme, "theme"), store_get(theme, "$theme", $$stores));
  const $_intlDayPopover = () => (validate_store(_intlDayPopover, "_intlDayPopover"), store_get(_intlDayPopover, "$_intlDayPopover", $$stores));
  const $buttonText = () => (validate_store(buttonText, "buttonText"), store_get(buttonText, "$buttonText", $$stores));
  let {
    buttonText,
    theme,
    _interaction,
    _intlDayPopover,
    _popupDate,
    _popupChunks
  } = getContext("state");
  let el = mutable_state();
  let style = mutable_state("");
  function position() {
    let dayEl = ancestor(get(el), 1);
    let bodyEl = ancestor(dayEl, 3);
    let popupRect = rect(get(el));
    let dayRect = rect(dayEl);
    let bodyRect = rect(bodyEl);
    set(style, "");
    let left;
    if (popupRect.width >= bodyRect.width) {
      left = bodyRect.left - dayRect.left;
      let right = dayRect.right - bodyRect.right;
      set(style, get(style) + `right:${right}px;`);
    } else {
      left = (dayRect.width - popupRect.width) / 2;
      if (dayRect.left + left < bodyRect.left) {
        left = bodyRect.left - dayRect.left;
      } else if (dayRect.left + left + popupRect.width > bodyRect.right) {
        left = bodyRect.right - dayRect.left - popupRect.width;
      }
    }
    set(style, get(style) + `left:${left}px;`);
    let top;
    if (popupRect.height >= bodyRect.height) {
      top = bodyRect.top - dayRect.top;
      let bottom = dayRect.bottom - bodyRect.bottom;
      set(style, get(style) + `bottom:${bottom}px;`);
    } else {
      top = (dayRect.height - popupRect.height) / 2;
      if (dayRect.top + top < bodyRect.top) {
        top = bodyRect.top - dayRect.top;
      } else if (dayRect.top + top + popupRect.height > bodyRect.bottom) {
        top = bodyRect.bottom - dayRect.top - popupRect.height;
      }
    }
    set(style, get(style) + `top:${top}px;`);
  }
  function reposition() {
    if (get(el)) {
      set(style, "");
      tick().then(() => {
        if ($_popupChunks().length) {
          position();
        } else {
          close();
        }
      });
    }
  }
  function close(e) {
    store_set(_popupDate, null);
  }
  function handlePointerDownOutside(e) {
    var _a;
    close();
    (_a = $_interaction().action) == null ? void 0 : _a.noClick();
  }
  legacy_pre_effect(() => $_popupChunks(), () => {
    if ($_popupChunks()) {
      reposition();
    }
  });
  legacy_pre_effect_reset();
  init();
  var div = root3();
  var div_1 = child(div);
  var time = child(div_1);
  template_effect(() => set_attribute(time, "datetime", toISOString($_popupDate(), 10)));
  action(time, ($$node, $$action_arg) => {
    var _a;
    return (_a = setContent) == null ? void 0 : _a($$node, $$action_arg);
  }, () => $_intlDayPopover().format($_popupDate()));
  var a = sibling(time, 2);
  var event_handler = derived(() => keyEnter(close));
  reset(div_1);
  var div_2 = sibling(div_1, 2);
  validate_each_keys($_popupChunks, (chunk) => chunk.event);
  each(div_2, 5, $_popupChunks, (chunk) => chunk.event, ($$anchor2, chunk) => {
    var fragment = comment();
    var node = first_child(fragment);
    Event_default(node, {
      get chunk() {
        return get(chunk);
      },
      inPopup: true
    });
    append($$anchor2, fragment);
  });
  reset(div_2);
  reset(div);
  bind_this(div, ($$value) => set(el, $$value), () => get(el));
  action(div, ($$node, $$action_arg) => {
    var _a;
    return (_a = outsideEvent) == null ? void 0 : _a($$node, $$action_arg);
  }, () => "pointerdown");
  effect(() => event("pointerdown", div, stopPropagation(function($$arg) {
    bubble_event.call(this, $$props, $$arg);
  })));
  effect(() => event("pointerdownoutside", div, handlePointerDownOutside));
  template_effect(() => {
    set_class(div, $theme().popup);
    set_attribute(div, "style", get(style));
    set_class(div_1, $theme().dayHead);
    set_attribute(a, "aria-label", $buttonText().close);
    set_class(div_2, $theme().events);
  });
  event("click", a, stopPropagation(close));
  event("keydown", a, function(...$$args) {
    apply(() => get(event_handler), this, $$args, Popup, [96, 24], true);
  });
  append($$anchor, div);
  return pop({ ...legacy_api() });
}
if (import.meta.hot) {
  Popup = hmr(Popup, () => Popup[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = Popup[HMR].source;
    set(Popup[HMR].source, module.default[HMR].original);
  });
}
var Popup_default = Popup;
mark_module_end(Popup);

// node_modules/@event-calendar/day-grid/src/Day.svelte
mark_module_start();
Day[FILENAME] = "node_modules/@event-calendar/day-grid/src/Day.svelte";
var root_2 = add_locations(template(`<div><!></div>`), Day[FILENAME], [[110, 8]]);
var root_3 = add_locations(template(`<div><!></div>`), Day[FILENAME], [[116, 8]]);
var root_6 = add_locations(template(`<a role="button" tabindex="0" aria-haspopup="true"></a>`), Day[FILENAME], [[132, 12]]);
var root4 = add_locations(template(`<div role="cell"><time></time> <div></div> <!> <!> <div></div> <!> <div><!></div></div>`), Day[FILENAME], [
  [
    91,
    0,
    [
      [98, 4],
      [103, 4],
      [120, 4],
      [128, 4]
    ]
  ]
]);
function Day($$anchor, $$props) {
  check_target(new.target);
  push($$props, false, Day);
  const $$stores = setup_stores();
  const $_hiddenEvents = () => (validate_store(_hiddenEvents, "_hiddenEvents"), store_get(_hiddenEvents, "$_hiddenEvents", $$stores));
  const $_today = () => (validate_store(_today, "_today"), store_get(_today, "$_today", $$stores));
  const $currentDate = () => (validate_store(currentDate, "currentDate"), store_get(currentDate, "$currentDate", $$stores));
  const $highlightedDates = () => (validate_store(highlightedDates, "highlightedDates"), store_get(highlightedDates, "$highlightedDates", $$stores));
  const $moreLinkContent = () => (validate_store(moreLinkContent, "moreLinkContent"), store_get(moreLinkContent, "$moreLinkContent", $$stores));
  const $_popupDate = () => (validate_store(_popupDate, "_popupDate"), store_get(_popupDate, "$_popupDate", $$stores));
  const $_popupChunks = () => (validate_store(_popupChunks, "_popupChunks"), store_get(_popupChunks, "$_popupChunks", $$stores));
  const $theme = () => (validate_store(theme, "theme"), store_get(theme, "$theme", $$stores));
  const $_interaction = () => (validate_store(_interaction, "_interaction"), store_get(_interaction, "$_interaction", $$stores));
  const $_intlDayCell = () => (validate_store(_intlDayCell, "_intlDayCell"), store_get(_intlDayCell, "$_intlDayCell", $$stores));
  let date = prop($$props, "date", 8);
  let chunks = prop($$props, "chunks", 8);
  let bgChunks = prop($$props, "bgChunks", 8);
  let longChunks = prop($$props, "longChunks", 8);
  let iChunks = prop($$props, "iChunks", 24, () => []);
  let dates = prop($$props, "dates", 8);
  let {
    date: currentDate,
    dayMaxEvents,
    highlightedDates,
    moreLinkContent,
    theme,
    _hiddenEvents,
    _intlDayCell,
    _popupDate,
    _popupChunks,
    _today,
    _interaction,
    _queue
  } = getContext("state");
  let el = mutable_state();
  let dayChunks = mutable_state(), dayBgChunks = mutable_state();
  let isToday = mutable_state();
  let otherMonth = mutable_state();
  let highlight = mutable_state();
  let hiddenEvents = mutable_state(/* @__PURE__ */ new Set());
  let moreLink = mutable_state("");
  let showPopup = mutable_state();
  let refs = mutable_state([]);
  function showMore() {
    store_set(_popupDate, date());
  }
  function setPopupChunks() {
    var _a;
    let nextDay = addDay(cloneDate(date()));
    let chunks2 = get(dayChunks).concat(((_a = longChunks()[date().getTime()]) == null ? void 0 : _a.chunks) || []);
    store_set(_popupChunks, chunks2.map((chunk) => assign({}, chunk, createEventChunk(chunk.event, date(), nextDay), { days: 1, dates: [date()] })).sort((a, b) => a.top - b.top));
  }
  function reposition() {
    runReposition(get(refs), get(dayChunks));
  }
  legacy_pre_effect(
    () => (get(dayChunks), deep_read_state(bgChunks()), datesEqual, deep_read_state(date()), get(hiddenEvents), deep_read_state(chunks())),
    () => {
      set(dayChunks, []);
      set(dayBgChunks, bgChunks().filter((bgChunk) => datesEqual(bgChunk.date, date())));
      get(hiddenEvents).clear();
      set(hiddenEvents, get(hiddenEvents));
      for (let chunk of chunks()) {
        if (datesEqual(chunk.date, date())) {
          get(dayChunks).push(chunk);
        }
      }
    }
  );
  legacy_pre_effect(
    () => (deep_read_state(date()), get(hiddenEvents)),
    () => {
      store_mutate(_hiddenEvents, untrack($_hiddenEvents)[date().getTime()] = get(hiddenEvents), untrack($_hiddenEvents));
    }
  );
  legacy_pre_effect(
    () => (datesEqual, deep_read_state(date()), $_today()),
    () => {
      set(isToday, datesEqual(date(), $_today()));
    }
  );
  legacy_pre_effect(
    () => (deep_read_state(date()), $currentDate(), $highlightedDates(), datesEqual),
    () => {
      set(otherMonth, strict_equals(date().getUTCMonth(), $currentDate().getUTCMonth(), false));
      set(highlight, $highlightedDates().some((d) => datesEqual(d, date())));
    }
  );
  legacy_pre_effect(
    () => ($_hiddenEvents(), get(hiddenEvents), $moreLinkContent(), isFunction),
    () => {
      if ($_hiddenEvents() && get(hiddenEvents).size) {
        let text = "+" + get(hiddenEvents).size + " more";
        if ($moreLinkContent()) {
          set(moreLink, isFunction($moreLinkContent()) ? $moreLinkContent()({ num: get(hiddenEvents).size, text }) : $moreLinkContent());
        } else {
          set(moreLink, text);
        }
      }
    }
  );
  legacy_pre_effect(
    () => ($_popupDate(), datesEqual, deep_read_state(date())),
    () => {
      set(showPopup, $_popupDate() && datesEqual(date(), $_popupDate()));
    }
  );
  legacy_pre_effect(
    () => (get(showPopup), deep_read_state(longChunks()), get(dayChunks), tick),
    () => {
      if (get(showPopup) && longChunks() && get(dayChunks)) {
        tick().then(setPopupChunks);
      }
    }
  );
  legacy_pre_effect(
    () => (get(el), setPayload, deep_read_state(date())),
    () => {
      if (get(el)) {
        setPayload(get(el), () => ({
          allDay: true,
          date: date(),
          resource: void 0,
          dayEl: get(el)
        }));
      }
    }
  );
  legacy_pre_effect_reset();
  init();
  var div = root4();
  const class_derived = derived_safe_equal(() => {
    var _a;
    return `${$theme().day ?? ""} ${((_a = $theme().weekdays) == null ? void 0 : _a[date().getUTCDay()]) ?? ""}${(get(isToday) ? " " + $theme().today : "") ?? ""}${(get(otherMonth) ? " " + $theme().otherMonth : "") ?? ""}${(get(highlight) ? " " + $theme().highlight : "") ?? ""}`;
  });
  var time = child(div);
  template_effect(() => set_attribute(time, "datetime", toISOString(date(), 10)));
  action(time, ($$node, $$action_arg) => {
    var _a;
    return (_a = setContent) == null ? void 0 : _a($$node, $$action_arg);
  }, () => $_intlDayCell().format(date()));
  var div_1 = sibling(time, 2);
  validate_each_keys(() => get(dayBgChunks), (chunk) => chunk.event);
  each(div_1, 5, () => get(dayBgChunks), (chunk) => chunk.event, ($$anchor2, chunk) => {
    var fragment = comment();
    var node = first_child(fragment);
    Event_default(node, {
      get chunk() {
        return get(chunk);
      }
    });
    append($$anchor2, fragment);
  });
  reset(div_1);
  var node_1 = sibling(div_1, 2);
  {
    var consequent = ($$anchor2) => {
      var div_2 = root_2();
      var node_2 = child(div_2);
      Event_default(node_2, {
        get chunk() {
          return iChunks()[2];
        }
      });
      reset(div_2);
      template_effect(() => set_class(div_2, $theme().events));
      append($$anchor2, div_2);
    };
    if_block(node_1, ($$render) => {
      if (iChunks()[2] && datesEqual(iChunks()[2].date, date())) $$render(consequent);
    });
  }
  var node_3 = sibling(node_1, 2);
  {
    var consequent_1 = ($$anchor2) => {
      var div_3 = root_3();
      var node_4 = child(div_3);
      Event_default(node_4, {
        get chunk() {
          return iChunks()[0];
        }
      });
      reset(div_3);
      template_effect(() => set_class(div_3, `${$theme().events ?? ""} ${$theme().preview ?? ""}`));
      append($$anchor2, div_3);
    };
    if_block(node_3, ($$render) => {
      if (iChunks()[0] && datesEqual(iChunks()[0].date, date())) $$render(consequent_1);
    });
  }
  var div_4 = sibling(node_3, 2);
  validate_each_keys(() => get(dayChunks), (chunk) => chunk.event);
  each(div_4, 7, () => get(dayChunks), (chunk) => chunk.event, ($$anchor2, chunk, i) => {
    var fragment_1 = comment();
    var node_5 = first_child(fragment_1);
    bind_this(
      Event_default(node_5, {
        get chunk() {
          return get(chunk);
        },
        get longChunks() {
          return longChunks();
        },
        get dates() {
          return dates();
        },
        $$legacy: true
      }),
      ($$value, i2) => mutate(refs, get(refs)[i2] = $$value),
      (i2) => {
        var _a;
        return (_a = get(refs)) == null ? void 0 : _a[i2];
      },
      () => [get(i)]
    );
    append($$anchor2, fragment_1);
  });
  reset(div_4);
  var node_6 = sibling(div_4, 2);
  {
    var consequent_2 = ($$anchor2) => {
      var fragment_2 = comment();
      var node_7 = first_child(fragment_2);
      Popup_default(node_7, {});
      append($$anchor2, fragment_2);
    };
    if_block(node_6, ($$render) => {
      if (get(showPopup)) $$render(consequent_2);
    });
  }
  var div_5 = sibling(node_6, 2);
  var node_8 = child(div_5);
  {
    var consequent_3 = ($$anchor2) => {
      var a_1 = root_6();
      var event_handler = derived(() => keyEnter(showMore));
      effect(() => event("click", a_1, stopPropagation(showMore)));
      effect(() => event("keydown", a_1, function(...$$args) {
        apply(() => get(event_handler), this, $$args, Day, [137, 28], true);
      }));
      effect(() => event("pointerdown", a_1, stopPropagation(function($$arg) {
        bubble_event.call(this, $$props, $$arg);
      })));
      action(a_1, ($$node, $$action_arg) => {
        var _a;
        return (_a = setContent) == null ? void 0 : _a($$node, $$action_arg);
      }, () => get(moreLink));
      append($$anchor2, a_1);
    };
    if_block(node_8, ($$render) => {
      if (get(hiddenEvents).size) $$render(consequent_3);
    });
  }
  reset(div_5);
  reset(div);
  bind_this(div, ($$value) => set(el, $$value), () => get(el));
  template_effect(() => {
    set_class(div, get(class_derived));
    set_class(time, $theme().dayHead);
    set_class(div_1, $theme().bgEvents);
    set_class(div_4, $theme().events);
    set_class(div_5, $theme().dayFoot);
  });
  event("pointerleave", div, function(...$$args) {
    apply(() => {
      var _a;
      return (_a = $_interaction().pointer) == null ? void 0 : _a.leave;
    }, this, $$args, Day, [95, 21]);
  });
  event("pointerdown", div, function(...$$args) {
    apply(() => {
      var _a;
      return (_a = $_interaction().action) == null ? void 0 : _a.select;
    }, this, $$args, Day, [96, 20]);
  });
  append($$anchor, div);
  bind_prop($$props, "reposition", reposition);
  return pop({
    get reposition() {
      return reposition;
    },
    ...legacy_api()
  });
}
if (import.meta.hot) {
  Day = hmr(Day, () => Day[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = Day[HMR].source;
    set(Day[HMR].source, module.default[HMR].original);
  });
}
var Day_default = Day;
mark_module_end(Day);

// node_modules/@event-calendar/day-grid/src/Week.svelte
mark_module_start();
Week[FILENAME] = "node_modules/@event-calendar/day-grid/src/Week.svelte";
var root5 = add_locations(template(`<div role="row"></div>`), Week[FILENAME], [[66, 0]]);
function Week($$anchor, $$props) {
  check_target(new.target);
  push($$props, false, Week);
  const $$stores = setup_stores();
  const $_events = () => (validate_store(_events, "_events"), store_get(_events, "$_events", $$stores));
  const $filterEventsWithResources = () => (validate_store(filterEventsWithResources, "filterEventsWithResources"), store_get(filterEventsWithResources, "$filterEventsWithResources", $$stores));
  const $resources = () => (validate_store(resources, "resources"), store_get(resources, "$resources", $$stores));
  const $hiddenDays = () => (validate_store(hiddenDays, "hiddenDays"), store_get(hiddenDays, "$hiddenDays", $$stores));
  const $_iEvents = () => (validate_store(_iEvents, "_iEvents"), store_get(_iEvents, "$_iEvents", $$stores));
  const $_hiddenEvents = () => (validate_store(_hiddenEvents, "_hiddenEvents"), store_get(_hiddenEvents, "$_hiddenEvents", $$stores));
  const $theme = () => (validate_store(theme, "theme"), store_get(theme, "$theme", $$stores));
  let dates = prop($$props, "dates", 8);
  let {
    _events,
    _iEvents,
    _queue2,
    _hiddenEvents,
    resources,
    filterEventsWithResources,
    hiddenDays,
    theme
  } = getContext("state");
  let chunks = mutable_state(), bgChunks = mutable_state(), longChunks = mutable_state(), iChunks = mutable_state([]);
  let start = mutable_state();
  let end = mutable_state();
  let refs = mutable_state([]);
  let debounceHandle = {};
  function reposition() {
    debounce(() => runReposition(get(refs), dates()), debounceHandle, _queue2);
  }
  legacy_pre_effect(
    () => (deep_read_state(dates()), addDay, cloneDate),
    () => {
      set(start, dates()[0]);
      set(end, addDay(cloneDate(dates().at(-1))));
    }
  );
  legacy_pre_effect(
    () => (get(chunks), get(bgChunks), $_events(), eventIntersects, get(start), get(end), $filterEventsWithResources(), $resources(), createEventChunk, bgEvent, prepareEventChunks, $hiddenDays()),
    () => {
      set(chunks, []);
      set(bgChunks, []);
      for (let event2 of $_events()) {
        if (eventIntersects(event2, get(start), get(end), $filterEventsWithResources() ? $resources() : void 0)) {
          let chunk = createEventChunk(event2, get(start), get(end));
          if (bgEvent(event2.display)) {
            if (event2.allDay) {
              get(bgChunks).push(chunk);
            }
          } else {
            get(chunks).push(chunk);
          }
        }
      }
      prepareEventChunks(get(bgChunks), $hiddenDays());
      set(longChunks, prepareEventChunks(get(chunks), $hiddenDays()));
      reposition();
    }
  );
  legacy_pre_effect(
    () => ($_iEvents(), eventIntersects, get(start), get(end), createEventChunk, prepareEventChunks, $hiddenDays()),
    () => {
      set(iChunks, $_iEvents().map((event2) => {
        let chunk;
        if (event2 && eventIntersects(event2, get(start), get(end))) {
          chunk = createEventChunk(event2, get(start), get(end));
          prepareEventChunks([chunk], $hiddenDays());
        } else {
          chunk = null;
        }
        return chunk;
      }));
    }
  );
  legacy_pre_effect(() => ($_hiddenEvents(), tick), () => {
    if ($_hiddenEvents()) {
      tick().then(reposition);
    }
  });
  legacy_pre_effect_reset();
  init();
  var div = root5();
  event("resize", $window, reposition);
  each(div, 5, dates, index, ($$anchor2, date, i) => {
    var fragment = comment();
    var node = first_child(fragment);
    bind_this(
      Day_default(node, {
        get date() {
          return get(date);
        },
        get chunks() {
          return get(chunks);
        },
        get bgChunks() {
          return get(bgChunks);
        },
        get longChunks() {
          return get(longChunks);
        },
        get iChunks() {
          return get(iChunks);
        },
        get dates() {
          return dates();
        },
        $$legacy: true
      }),
      ($$value, i2) => mutate(refs, get(refs)[i2] = $$value),
      (i2) => {
        var _a;
        return (_a = get(refs)) == null ? void 0 : _a[i2];
      },
      () => [i]
    );
    append($$anchor2, fragment);
  });
  reset(div);
  template_effect(() => set_class(div, $theme().days));
  append($$anchor, div);
  return pop({ ...legacy_api() });
}
if (import.meta.hot) {
  Week = hmr(Week, () => Week[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = Week[HMR].source;
    set(Week[HMR].source, module.default[HMR].original);
  });
}
var Week_default = Week;
mark_module_end(Week);

// node_modules/@event-calendar/day-grid/src/Body.svelte
mark_module_start();
Body[FILENAME] = "node_modules/@event-calendar/day-grid/src/Body.svelte";
var root6 = add_locations(template(`<div><div></div></div>`), Body[FILENAME], [[25, 0, [[29, 4]]]]);
function Body($$anchor, $$props) {
  check_target(new.target);
  push($$props, false, Body);
  const $$stores = setup_stores();
  const $hiddenDays = () => (validate_store(hiddenDays, "hiddenDays"), store_get(hiddenDays, "$hiddenDays", $$stores));
  const $_hiddenEvents = () => (validate_store(_hiddenEvents, "_hiddenEvents"), store_get(_hiddenEvents, "$_hiddenEvents", $$stores));
  const $dayMaxEvents = () => (validate_store(dayMaxEvents, "dayMaxEvents"), store_get(dayMaxEvents, "$dayMaxEvents", $$stores));
  const $_viewDates = () => (validate_store(_viewDates, "_viewDates"), store_get(_viewDates, "$_viewDates", $$stores));
  const $_bodyEl = () => (validate_store(_bodyEl, "_bodyEl"), store_get(_bodyEl, "$_bodyEl", $$stores));
  const $theme = () => (validate_store(theme, "theme"), store_get(theme, "$theme", $$stores));
  let {
    _bodyEl,
    _viewDates,
    _hiddenEvents,
    dayMaxEvents,
    hiddenDays,
    theme
  } = getContext("state");
  let weeks = mutable_state();
  let days2 = mutable_state();
  legacy_pre_effect(
    () => (get(weeks), get(days2), $hiddenDays(), $dayMaxEvents(), $_viewDates()),
    () => {
      set(weeks, []);
      set(days2, 7 - $hiddenDays().length);
      store_set(_hiddenEvents, {});
      $dayMaxEvents();
      for (let i = 0; i < $_viewDates().length / get(days2); ++i) {
        let dates = [];
        for (let j = 0; j < get(days2); ++j) {
          dates.push($_viewDates()[i * get(days2) + j]);
        }
        get(weeks).push(dates);
      }
    }
  );
  legacy_pre_effect_reset();
  init();
  var div = root6();
  var div_1 = child(div);
  each(div_1, 5, () => get(weeks), index, ($$anchor2, dates) => {
    var fragment = comment();
    var node = first_child(fragment);
    Week_default(node, {
      get dates() {
        return get(dates);
      }
    });
    append($$anchor2, fragment);
  });
  reset(div_1);
  reset(div);
  bind_this(div, ($$value) => store_set(_bodyEl, $$value), () => $_bodyEl());
  template_effect(() => {
    set_class(div, `${$theme().body ?? ""}${(strict_equals($dayMaxEvents(), true) ? " " + $theme().uniform : "") ?? ""}`);
    set_class(div_1, $theme().content);
  });
  append($$anchor, div);
  return pop({ ...legacy_api() });
}
if (import.meta.hot) {
  Body = hmr(Body, () => Body[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = Body[HMR].source;
    set(Body[HMR].source, module.default[HMR].original);
  });
}
var Body_default = Body;
mark_module_end(Body);

// node_modules/@event-calendar/day-grid/src/View.svelte
mark_module_start();
View[FILENAME] = "node_modules/@event-calendar/day-grid/src/View.svelte";
var root7 = add_locations(template(`<!> <!>`, 1), View[FILENAME], []);
function View($$anchor, $$props) {
  check_target(new.target);
  push($$props, false, View);
  var fragment = root7();
  var node = first_child(fragment);
  Header_default(node, {});
  var node_1 = sibling(node, 2);
  Body_default(node_1, {});
  append($$anchor, fragment);
  return pop({ ...legacy_api() });
}
if (import.meta.hot) {
  View = hmr(View, () => View[HMR].source);
  import.meta.hot.accept((module) => {
    module.default[HMR].source = View[HMR].source;
    set(View[HMR].source, module.default[HMR].original);
  });
}
var View_default = View;
mark_module_end(View);

// node_modules/@event-calendar/day-grid/src/index.js
var src_default = {
  createOptions(options) {
    options.dayMaxEvents = false;
    options.dayCellFormat = { day: "numeric" };
    options.dayPopoverFormat = { month: "long", day: "numeric", year: "numeric" };
    options.moreLinkContent = void 0;
    options.buttonText.dayGridMonth = "month";
    options.buttonText.close = "Close";
    options.theme.uniform = "ec-uniform";
    options.theme.dayFoot = "ec-day-foot";
    options.theme.popup = "ec-popup";
    options.view = "dayGridMonth";
    options.views.dayGridMonth = {
      buttonText: btnTextMonth,
      component: View_default,
      dayHeaderFormat: { weekday: "short" },
      dayHeaderAriaLabelFormat: { weekday: "long" },
      displayEventEnd: false,
      duration: { months: 1 },
      theme: themeView("ec-day-grid ec-month-view"),
      titleFormat: { year: "numeric", month: "long" }
    };
  },
  createStores(state) {
    state._days = days(state);
    state._intlDayCell = intl(state.locale, state.dayCellFormat);
    state._intlDayPopover = intl(state.locale, state.dayPopoverFormat);
    state._hiddenEvents = writable({});
    state._popupDate = writable(null);
    state._popupChunks = writable([]);
  }
};
export {
  src_default as default
};
//# sourceMappingURL=@event-calendar_day-grid.js.map
